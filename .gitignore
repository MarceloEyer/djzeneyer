# ===========================================
# DJ Zen Eyer Theme .gitignore v1.3.0 FINAL
# ===========================================

# 📦 Node dependencies (NUNCA versionar)
node_modules/
.pnpm-store/
pnpm-lock.yaml
yarn.lock
.yarn/
.npm

# 🔨 Build output (regenerado com npm run build)
# dist/ é IGNORADO aqui porque é gerado pelo build
# Mas será enviado ao servidor via GitHub Actions artifact
dist/
dist-ssr/
build/
.vite/
*.tsbuildinfo
.github/assets

# 🔐 SECRETS - NUNCA COMMITAR!
#.env
#.env.local
.env.production.local
.env.development.local
!*.env.example  # EXCEÇÃO: Permite versionar .env.example

# 💻 IDE & Sistema (ignora configurações locais)
.vscode/settings.json
.vscode/launch.json
.idea/
.DS_Store
Thumbs.db
*.sublime-workspace
*.swp
*.swo
*~

# 📝 Logs & cache (ignora arquivos gerados)
logs/
*.log
npm-debug.log*
.eslintcache
.cache/
.turbo/
coverage/

# 📁 Diretórios temporários
tmp/
temp/

# 📄 Arquivos de sistema
*.bak
*.backup
*.orig

# ============================================
# WHITELIST - Forçar versionamento
# ============================================
!.gitkeep
!.env.example
!.vscode/extensions.json  # Extensões recomendadas para a equipe

# ⚠️ NÃO IGNORAR (SEMPRE VERSIONAR):
# - .github/workflows/ (workflows de CI/CD)
# - config/ (configurações do projeto)
# - src/ (código-fonte React/TypeScript)
# - inc/ (configurações do WordPress)
# - plugins/ (plugins customizados)
# - functions.php, index.php, style.css (arquivo do tema)
