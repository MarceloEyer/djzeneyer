name: 🚀 Deploy para Hostinger (direto no código para teste)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout do Repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📥 Instalar dependências
        run: npm install

      - name: 🏗️ Build do Projeto
        run: npm run build

      - name: 🧾 Listar arquivos do build
        run: ls -l dist

      - name: 📤 Deploy via SCP (com dados direto do painel)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 147.79.84.222                 # DIRETO DA IMAGEM
          username: u790739895                # DIRETO DA IMAGEM
          password: ${{ secrets.SSH_PASSWORD }} # Usa secret SÓ para senha
          port: 65002                         # DIRETO DA IMAGEM
          source: "dist/*"
          target: "/home/u790739895/domains/djzeneyer.com/public_html/"
          strip_components: 1
          overwrite: true
          rm: false
          debug: true
          timeout: 180s

      - name: 🔎 Listar arquivos no Hostinger após deploy
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.SSH_PASSWORD }}
          port: 65002
          script: |
            echo "Conteúdo em /home/u790739895/domains/djzeneyer.com/public_html/"
            ls -la /home/u790739895/domains/djzeneyer.com/public_html/
