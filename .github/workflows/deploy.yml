name: 🚀 Build & Deploy to Hostinger (via SSH)

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  build:
    name: 📦 Build do Projeto
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout do Repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📥 Instalar Dependências
        run: npm ci

      - name: 🏗️ Compilar o Projeto (Build)
        run: npm run build
        env:
          VITE_WP_SITE_URL: ${{ secrets.VITE_WP_SITE_URL }}
          VITE_WP_REST_URL: ${{ secrets.VITE_WP_REST_URL }}
          VITE_WC_CONSUMER_KEY: ${{ secrets.VITE_WC_CONSUMER_KEY }}
          VITE_WC_CONSUMER_SECRET: ${{ secrets.VITE_WC_CONSUMER_SECRET }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}

      - name: 📤 Upload do Artefato (dist/)
        uses: actions/upload-artifact@v4
        with:
          name: dist-folder
          path: ./dist

  deploy:
    name: 🚚 Deploy via SSH para Hostinger
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 📂 Checkout do Repositório
        uses: actions/checkout@v4

      - name: 📥 Download do Artefato (dist/)
        uses: actions/download-artifact@v4
        with:
          name: dist-folder
          path: ./dist

      - name: 🎨 Deploy de arquivos públicos para a raiz
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./public/*"
          target: ${{ secrets.SSH_PUBLIC_PATH }}
          overwrite: true

      - name: 📡 Deploy do app React (dist/) para a pasta do tema
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./dist/*"
          target: ${{ secrets.SSH_THEME_PATH }}/dist
          overwrite: true
          rm: true

      - name: 📦 Deploy dos arquivos do tema WordPress
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./*"
          target: ${{ secrets.SSH_THEME_PATH }}
          exclude: |
            .git,
            .github,
            node_modules,
            dist,
            public,
            .bolt,
            package*.json,
            *.md,
            *.lock,
            *.ts,
            *.tsx
          overwrite: true
