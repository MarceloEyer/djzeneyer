name: 🚀 Deploy Corrigido para Hostinger (Tema zentheme)

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    name: 📦 Build & Deploy (Hostinger)
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout do Repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📥 Instalar Dependências
        run: npm ci

      - name: 🏗️ Compilar o Projeto
        run: |
          echo "🔧 Iniciando build..."
          npm run build
        env:
          VITE_WP_SITE_URL: ${{ secrets.VITE_WP_SITE_URL }}
          VITE_WP_REST_URL: ${{ secrets.VITE_WP_REST_URL }}
          VITE_WC_CONSUMER_KEY: ${{ secrets.VITE_WC_CONSUMER_KEY }}
          VITE_WC_CONSUMER_SECRET: ${{ secrets.VITE_WC_CONSUMER_SECRET }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}

      - name: 🧾 Listar arquivos gerados
        run: |
          echo "📁 Conteúdo da pasta dist/"
          ls -la dist

      - name: 💣 Limpar e preparar pasta remota
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          script_stop: true
          script: |
            echo "🚨 Limpando diretório remoto do tema..."
            rm -rf /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/*
            echo "✅ Pasta limpa. Recriando estrutura..."
            mkdir -p /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/
            echo "📂 Verificando pasta:"
            ls -la /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/

      - name: 📤 Enviar arquivos (SCP com debug)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          source: "dist/*"
          target: "/home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/"
          strip_components: 0
          overwrite: true
          rm: false
          timeout: 180s
          debug: true

      - name: 🔎 Verificar upload remoto
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: 65002
          script_stop: true
          script: |
            echo "🧭 Verificando conteúdo do tema:"
            ls -la /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/
            echo "📦 Total de arquivos:"
            find /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/ -type f | wc -l
            echo "📂 Estrutura do tema:"
            find /home/u790739895/domains/djzeneyer.com/public_html/wp-content/themes/zentheme/ -maxdepth 3
