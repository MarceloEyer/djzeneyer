name: 🚀 Deploy to Hostinger via SSH

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    name: 🧱 Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout do Repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📥 Instalar Dependências
        run: npm ci

      - name: 🏗️ Build do Projeto
        run: npm run build
        env:
          VITE_WP_SITE_URL: ${{ secrets.VITE_WP_SITE_URL }}
          VITE_WP_REST_URL: ${{ secrets.VITE_WP_REST_URL }}
          VITE_WC_CONSUMER_KEY: ${{ secrets.VITE_WC_CONSUMER_KEY }}
          VITE_WC_CONSUMER_SECRET: ${{ secrets.VITE_WC_CONSUMER_SECRET }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}

      - name: 🧾 Listar arquivos gerados
        run: |
          echo "📁 Conteúdo da pasta dist/"
          ls -la dist

      - name: 📤 Deploy via SCP para Hostinger
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.SSH_PASSWORD }}
          port: 65002
          source: "dist/*"
          target: "/home/u790739895/domains/djzeneyer.com/public_html/"
          strip_components: 1
          overwrite: true
          rm: false
          timeout: 180s
          debug: true

      - name: 🔎 Verificar upload remoto
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: 147.79.84.222
          username: u790739895
          password: ${{ secrets.SSH_PASSWORD }}
          port: 65002
          script: |
            echo "🧭 Verificando arquivos em /home/u790739895/domains/djzeneyer.com/public_html/"
            ls -la /home/u790739895/domains/djzeneyer.com/public_html/
            echo "📦 Total de arquivos:"
            find /home/u790739895/domains/djzeneyer.com/public_html/ -type f | wc -l
